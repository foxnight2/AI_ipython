
# import torch.optim as optim


optimizer = globals()['optim'].SGD(model.parameters(), lr=0.1)

optimizer = torch.optim.SGD(model.parameters(), lr=0.1)

lr_scheduler = torch.optim.lr_scheduler.MultiStepLR(optimizer, milestones=[1, 2], gamma=0.1)